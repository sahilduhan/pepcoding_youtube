#include <bits/stdc++.h>
using namespace std;
int main()
{

    vector<vector<int>> test;
    for (int i = 0; i < test.size(); i++)
    {
        int num;
        test.push_back(vector<int>());
        for (int j = 0; j < num; j++)
        {
            int val;
            cin >> val;
            test[i].push_back(val);
        }
    }

    return 0;
}