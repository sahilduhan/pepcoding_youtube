#include <bits/stdc++.h>
using namespace std;
int main()
{
    string str[] = {"(", "a", "+", "b", ")", "(", "(", "c", "+", "d", ")", ")"};
    int size = sizeof(str) / sizeof(str[0]);
    stack<char> st;
    while (size)
    {
        // for (int i = 0; i < size; i++)
        // {
        //     string c = str[i];
        //     if (c == ")")
        //     {
        //         if (st.top() == "(")
        //     }
        // }
    }
    return 0;
}