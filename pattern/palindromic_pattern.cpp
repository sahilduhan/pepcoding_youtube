#include <bits/stdc++.h>
using namespace std;
int main()
{
    int rows = 5;
    for (int i = 0; i < rows; i++)
    {
        for (int k = 0; k < rows - i; k++)
        {
            cout << " ";
        }
        for (int j = 0; j < i; j++)
        {
            cout << i << " ";
        }
        // for(int l=)
    }

    return 0;
}